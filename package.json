{
  "name": "ol-react",
  "version": "0.0.4",
  "description": "React interface for openlayers",
  "main": "lib/index.js",
  "scripts": {
  },
  "author": "richard.hills@gmail.com",
  "scripts": {
    "compile": "babel -d lib/ src/",
	"preinstall": "if [ ! -d lib/ ]; then npm run prepublish; fi",
    "prepublish": "npm run compile"
  },
  "devDependencies": {
    "babel": "^4.6.0",
    "babel-loader": "^6.2.0"
  },
  "peerDependencies": {
    "react": "^15.0.1",
    "react-dom": "^15.0.1"
  },
  "dependencies": {
    "openlayers": "^3.15.1"
  }
}
